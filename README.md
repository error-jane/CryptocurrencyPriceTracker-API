This is a CryptocurrencyPriceTracker API project with Node.js

## Getting Started

First, run the development server:

```bash
docker compose up -d
# and
docker compose build
```

You can connect API with [http://localhost:3000](http://localhost:3000).